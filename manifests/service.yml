apiVersion: v1
kind: Service
metadata:
    name: "minibytescloudcluster-9265"
    labels:
        app: "minibytescloudcluster-9265"
spec:
    type: ClusterIP
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "minibytescloudcluster-9265"
apiVersion: v1
kind: Service
metadata:
    name: "minibytescloudcluster-7fec"
    labels:
        app: "minibytescloudcluster-7fec"
spec:
    type: ClusterIP
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "minibytescloudcluster-7fec"